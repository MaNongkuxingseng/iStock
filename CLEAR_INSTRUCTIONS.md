# 📋 iStock环境准备 - 清晰可执行说明

## 🎯 **决策点: 选择数据库方案**

### **方案1: 使用SQLite (推荐开发环境)**
**优点**: 无需安装，零配置，文件数据库
**缺点**: 功能有限，不适合生产环境

### **方案2: 安装PostgreSQL (推荐生产环境)**
**优点**: 功能完整，性能好，支持复杂查询
**缺点**: 需要安装和配置

### **方案3: 使用Docker运行PostgreSQL**
**优点**: 环境隔离，易于管理
**缺点**: 需要Docker运行

## 🚀 **清晰执行步骤**

### **步骤1: 检查当前状态**
```bash
# 运行检查脚本
check_postgres.bat
```

**预期结果:**
- ✅ PostgreSQL已安装并运行 → 跳到步骤3
- ❌ PostgreSQL未安装 → 继续步骤2
- ⚠️  PostgreSQL已安装但未运行 → 启动服务

### **步骤2: 选择并安装数据库**

#### **选项A: 安装PostgreSQL (5-10分钟)**
1. 访问: https://www.postgresql.org/download/windows/
2. 下载PostgreSQL 16安装程序
3. 运行安装程序，使用以下设置:
   - 安装目录: `C:\Program Files\PostgreSQL\16`
   - 端口: `5432` (默认)
   - 密码: 设置并记住密码
   - 本地化: Chinese (Simplified), China
4. 完成安装

#### **选项B: 使用SQLite (2分钟)**
1. 运行: `install_postgres.bat`
2. 选择选项2 (使用SQLite)
3. 脚本会自动配置

#### **选项C: 使用Docker (3分钟)**
1. 确保Docker已配置 (运行 `docker_fullpath.bat --version`)
2. 运行: `install_postgres.bat`
3. 选择选项3 (使用Docker)
4. 脚本会自动启动PostgreSQL容器

### **步骤3: 验证数据库连接**
```bash
# 运行验证脚本
check_postgres.bat
```

**应该看到:**
```
✅ PostgreSQL服务正在运行
✅ 可以连接到PostgreSQL数据库
```

### **步骤4: 安装iStock依赖**
```bash
# 1. 安装Python依赖
pip install -r backend/requirements.txt

# 2. 安装前端依赖
cd frontend
npm install
```

### **步骤5: 运行测试验证**
```bash
# 运行真实测试
python run_real_tests_en.py
```

## 🔧 **故障排除**

### **问题1: PostgreSQL连接失败**
**解决方案:**
```bash
# 检查服务状态
net start | findstr postgresql

# 启动服务
net start postgresql-x64-16

# 或使用SQLite方案
install_postgres.bat
选择选项2
```

### **问题2: pip安装失败**
**解决方案:**
```bash
# 升级pip
python -m pip install --upgrade pip

# 使用国内镜像
pip install -r backend/requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
```

### **问题3: npm安装失败**
**解决方案:**
```bash
# 清理缓存
npm cache clean --force

# 使用淘宝镜像
npm install --registry=https://registry.npmmirror.com
```

## 📊 **环境检查清单**

### **必需组件**
- [ ] Python 3.8+ (`python --version`)
- [ ] pip 最新版 (`pip --version`)
- [ ] Node.js 14+ (`node --version`)
- [ ] npm 最新版 (`npm --version`)

### **数据库 (三选一)**
- [ ] PostgreSQL 已安装运行
- [ ] SQLite 已配置
- [ ] Docker + PostgreSQL 容器运行

### **可选组件**
- [ ] Docker Desktop 已安装
- [ ] Git 已安装 (`git --version`)
- [ ] 代码编辑器 (VS Code等)

## 🛠️ **快速启动脚本**

### **一键环境检查**
```bash
# 运行完整环境检查
check_postgres.bat
```

### **一键依赖安装**
```bash
# 安装所有依赖
install_dependencies.bat
```

### **一键服务启动**
```bash
# 启动所有服务
start_all_services.bat
```

## 📞 **技术支持**

### **遇到问题时的步骤:**
1. **运行检查脚本** - 获取详细错误信息
2. **查看错误日志** - 脚本会生成日志文件
3. **搜索解决方案** - 常见问题有预设解决方案
4. **联系支持** - 提供错误信息和日志

### **需要提供的帮助信息:**
1. 运行 `check_postgres.bat` 的输出
2. 错误消息的完整内容
3. 操作系统版本信息
4. 已安装的软件版本

## ✅ **成功标准**

### **环境准备完成标志:**
1. ✅ `python run_real_tests_en.py` 通过率 > 80%
2. ✅ 后端服务可启动: `http://localhost:8000/health`
3. ✅ 前端服务可启动: `http://localhost:3000`
4. ✅ 数据库连接正常

### **验证命令:**
```bash
# 完整验证
python run_real_tests_en.py

# 单独验证
check_postgres.bat
python --version
node --version
docker_fullpath.bat --version
```

---

## 🎯 **立即行动建议**

### **如果您是开发者:**
1. **选择SQLite方案** - 快速开始开发
2. **安装依赖** - 运行pip和npm安装
3. **运行测试** - 验证环境就绪
4. **开始编码** - 基于测试结果开发

### **如果您要部署生产环境:**
1. **安装PostgreSQL** - 完整功能支持
2. **配置环境变量** - 设置数据库连接
3. **运行完整测试** - 确保所有功能正常
4. **部署到服务器** - 使用Docker或直接部署

### **最简方案 (5分钟内启动):**
```bash
# 1. 选择SQLite
install_postgres.bat
选择2

# 2. 安装依赖
pip install -r backend/requirements.txt
cd frontend && npm install

# 3. 验证
python run_real_tests_en.py
```

**环境问题已明确，请根据上述清晰说明执行！** 🚀